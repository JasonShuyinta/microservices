spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/JasonShuyinta/cloud-config-server

server:
  port: 9196

#eureka:
#  client:
#    register-with-eureka: true
#    fetch-registry: true
#    service-url:
      #defaultZone: http://localhost:8761/eureka/
#      defaultZone: http://service-registry:8761/eureka/
#  instance:
#    hostname: service-registry
    #hostname: localhost